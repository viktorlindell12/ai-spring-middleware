spring:
  application:
    name: ai-middleware

server:
  port: 8080

llm:
  api-key: ${OPENROUTER_API_KEY}
  base-url: https://openrouter.ai/api/v1

springdoc:
  swagger-ui:
    path: /swagger-ui.html